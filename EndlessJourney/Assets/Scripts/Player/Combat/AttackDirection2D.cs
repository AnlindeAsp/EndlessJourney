namespace EndlessJourney.Player
{
    /// <summary>
    /// Direction snapshot for one player attack action.
    /// </summary>
    public enum AttackDirection2D
    {
        Forward,
        Up,
        Down
    }
}
